{
  "plugin_id": "format.example",
  "version": "1.0.0",
  "kind": "format",
  "entrypoint": "format-example",
  "description": "Example plugin demonstrating all plugin features (noop for documentation only)",
  "capabilities": {
    "inputs": ["file"],
    "outputs": ["artifact.kind:example"]
  },
  "ir_support": {
    "can_extract": true,
    "can_emit": true,
    "loss_class": "L1",
    "formats": ["example"]
  }
}
